// 10. Use join() to create a single string of names separated by commas. 

let enames = ["rahul", "sonia", "priya", "modi"]
let names=enames.join(" ")//provides space betwwn the elements and join
let ename=enames.join()//print with commas as string
console.log(ename);
console.log(names);

